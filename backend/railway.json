{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "env -u NPM_TOKEN -u NPM_CONFIG__AUTH -u NPM_CONFIG__AUTH_TOKEN -u NODE_AUTH_TOKEN npm ci --include=dev && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
